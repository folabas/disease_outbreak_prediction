"""Minimal ML package to train baselines and produce reports.

This package provides simple, stable baselines so you can run the
requested commands without external ML dependencies. It reads
`data/outbreakiq_training_data_filled.csv` and outputs metrics and
health checks under `reports/production/`.
"""