Processors

Scripts for cleaning and transforming raw datasets into analysis-ready CSVs.

- `clean_ncdc_data.py`: Cleans `ncdc_outbreaks.csv` and writes `data/ncdc_outbreaks_clean.csv`.

Run from project root to keep relative paths consistent.